{
  "name": "todo-afl-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Testes end-to-end (Playwright) cobrindo UI + HTTP + banco.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "better-sqlite3": "^11.7.0"
  }
}
